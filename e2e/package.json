{
  "scripts": {
    "open": "STAGE=local-confirm cypress open",
    "test:single": "STAGE=local-confirm cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^12.4.1",
    "cypress-mochawesome-reporter": "^3.2.3"
  }
}
